package main

import (
	"Array/problems"
	"fmt"
)

func main() {
	// nums := []int{0, 1, 2, 0, 2, 1, 1, 0, 2}
	// fmt.Println(problems.DutchFlagPartition(1, nums))

	// digits := []int{1, 2, 9}
	// fmt.Print(problems.PlusOne(digits))

	num1 := "123"
	num2 := "456"
	fmt.Printf("%s", problems.Multiply(num1, num2))
}
